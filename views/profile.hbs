 <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">Mi Espacio</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
              <li class="nav-item">
                <a class="nav-link"  href="/">Home</a>
              </li>
              <li class="nav-item">
                <a class="nav-link"  href="/characters">Games</a>
              </li>
              <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/profile">Profile</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/auth/logout">logout</a>
              </li>
            </ul>
          </div>
        </div>
      </nav>
<h1>bienvenido {{user.username}}</h1>
 


{{#each user.favorites}}
<br>
<br>
<img src={{thumbnail}} alt={{title}}>
<p>{{apiId}}</p>
<p>{{title}}</p>
<p>{{genre}}</p>
<p>{{platform}}</p>
<p>{{developer}}</p>
<p>{{release_date}}</p>
<p>{{short_description}}</p>
<p>{{freetogame_profile_url}}</p>

{{!-- puntuación --}}
<div class="card">
  <div class="card-body">
     <h3 class="bi bi-controller"  onclick="calificar(this)" id="1{{_id}}" style="cursor: pointer; display: inline;"><input value="1" type="hidden" name="calificar"></input></h3>
     <h3 class="bi bi-controller"  onclick="calificar(this)" id="2{{_id}}" style="cursor: pointer; display: inline;"><input value="2" type="hidden" name="calificar"></input></h3>
     <h3 class="bi bi-controller"  onclick="calificar(this)" id="3{{_id}}" style="cursor: pointer; display: inline;"><input value="3" type="hidden" name="calificar"></input></h3>
     <h3 class="bi bi-controller"  onclick="calificar(this)" id="4{{_id}}" style="cursor: pointer; display: inline;"><input value="4" type="hidden" name="calificar"></input></h3>
     <h3 class="bi bi-controller"  onclick="calificar(this)" id="5{{_id}}" style="cursor: pointer; display: inline;"><input value="5" type="hidden" name="calificar"></input></h3>
       <br>
       <br>
       <button class="btn btn-primary">Calificar</button>
  </div>
  </div>
<script>
  let contador;
  function calificar(item) {
    //console.log(item);
    contador =item.id[0]; // captura el primer caracter
    let nombre = item.id.substring(1); // 4 'estrella ' captura todo menos el primer caracter
    for(let i = 0; i < 5; i++) {
    console.log(i);
      if(i< contador) {
        document.getElementById((i+1) + nombre).style.color="orange";
      } else {
        document.getElementById((i+1) + nombre).style.color="#000000";
      }
    }
  }
</script>
{{!-- puntuación --}}

{{!-- boton de borrar --}}
<form action = "/delete-favorite" method="POST">
    <input type="hidden" name="id" value={{_id}}>
  <br>
  <br>  
<button type="submit">borrar favorito</button>
</form>
{{!-- boton de borrar --}}
{{/each}}


